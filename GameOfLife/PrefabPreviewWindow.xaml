<Window x:Class="GameOfLife.PrefabPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:GameOfLife.Converters"
        Title="Prefab auswählen"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <conv:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        
        <DataTemplate x:Key="PatternTemplate">
            <Border BorderBrush="#444" BorderThickness="1" Margin="8" Padding="10" Background="#1E1E1E" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="{Binding Group}" HorizontalAlignment="Center" Foreground="#CCCCCC" Margin="0,2,0,10"/>
                    <Border BorderBrush="#555" BorderThickness="1" CornerRadius="4" Background="#2B2B2B" Padding="6">
                        <Viewbox Stretch="Uniform" MaxHeight="250" MaxWidth="250" HorizontalAlignment="Center">
                            <Grid>
                                <Canvas Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}" Background="#111111">
                                    <ItemsControl ItemsSource="{Binding GridLines}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}" Stroke="#333" StrokeThickness="0.7"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <ItemsControl ItemsSource="{Binding Cells}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter">
                                                <Setter Property="Canvas.Left" Value="{Binding Left}"/>
                                                <Setter Property="Canvas.Top" Value="{Binding Top}"/>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Rectangle Width="{Binding Size}"
                                                           Height="{Binding Size}"
                                                           Stroke="#444"
                                                           StrokeThickness="0.5">
                                                    <Rectangle.Fill>
                                                        <SolidColorBrush Color="{Binding IsAlive, Converter={StaticResource BoolToColorConverter}}"/>
                                                    </Rectangle.Fill>
                                                </Rectangle>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Canvas>
                            </Grid>
                        </Viewbox>
                    </Border>
                    <Button Content="Select"
                            Command="{Binding DataContext.SelectPatternCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"
                            HorizontalAlignment="Center"
                            Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="PatternsList"
                         ItemsSource="{Binding Patterns}"
                         ItemTemplate="{StaticResource PatternTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Click="CancelButton_Click" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>
